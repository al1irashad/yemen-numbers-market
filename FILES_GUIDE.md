# دليل الملفات الرئيسية

## 📁 هيكل المشروع

```
yemen-numbers-market/
├── client/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Home.tsx              ← الصفحة الرئيسية
│   │   │   ├── AdminLogin.tsx        ← صفحة تسجيل دخول Admin
│   │   │   └── AdminDashboard.tsx    ← لوحة التحكم
│   │   ├── components/
│   │   │   ├── Header.tsx            ← الشريط العلوي
│   │   │   ├── NumberCard.tsx        ← بطاقة الرقم
│   │   │   ├── FilterBar.tsx         ← شريط الفلاتر
│   │   │   ├── SearchSection.tsx     ← قسم البحث
│   │   │   ├── SellNumberForm.tsx    ← نموذج البيع
│   │   │   ├── ContactSection.tsx    ← قسم التواصل
│   │   │   └── Footer.tsx            ← الفوتر
│   │   ├── hooks/
│   │   │   ├── useNumbers.ts         ← إدارة الفلاتر والترتيب
│   │   │   └── useNumbersStorage.ts  ← إدارة البيانات والتخزين
│   │   ├── App.tsx                   ← التطبيق الرئيسي
│   │   └── index.css                 ← الأنماط العامة
│   └── index.html                    ← ملف HTML الرئيسي
└── GUIDE.md                          ← هذا الدليل
```

---

## 📄 شرح الملفات الرئيسية

### 1. **Home.tsx** - الصفحة الرئيسية
**المسؤول عن:**
- عرض جميع الأرقام المتاحة
- الفلاتر والبحث
- قسم البيع والتواصل

**الملفات المستخدمة:**
```typescript
import { useNumbersStorage } from "@/hooks/useNumbersStorage";
```

**كيفية التعديل:**
- لتغيير النصوص: ابحث عن النص الذي تريد تغييره
- لإضافة أقسام جديدة: أضفها قبل Footer

---

### 2. **AdminLogin.tsx** - صفحة تسجيل الدخول
**المسؤول عن:**
- التحقق من كلمة المرور
- إعادة التوجيه إلى لوحة التحكم

**كلمة المرور:**
```typescript
// في useNumbersStorage.ts
const ADMIN_PASSWORD = "admin123";
```

---

### 3. **AdminDashboard.tsx** - لوحة التحكم
**المسؤول عن:**
- إضافة أرقام جديدة
- حذف الأرقام
- تعديل الأرقام
- عرض جميع الأرقام في جدول

**الوظائف الرئيسية:**
```typescript
addNumber()      // إضافة رقم جديد
deleteNumber()   // حذف رقم
updateNumber()   // تعديل رقم
```

---

### 4. **useNumbersStorage.ts** - Hook إدارة البيانات
**المسؤول عن:**
- حفظ البيانات في localStorage
- تحميل البيانات من localStorage
- التحقق من كلمة المرور

**البيانات المخزنة:**
```typescript
interface Number {
  id: string;           // معرّف فريد
  number: string;       // رقم الهاتف
  price: number;        // السعر
  provider: string;     // يمن موبايل أو سبأفون
  description?: string; // وصف اختياري
}
```

---

### 5. **SellNumberForm.tsx** - نموذج البيع
**المسؤول عن:**
- استقبال طلبات البيع من العملاء
- إرسال الطلبات إلى الواتساب

**رقم الواتساب:**
```typescript
const WHATSAPP_NUMBER = "+967711411141";
```

---

### 6. **FilterBar.tsx** - شريط الفلاتر
**المسؤول عن:**
- تصفية الأرقام حسب الشركة
- البحث عن أرقام محددة
- ترتيب الأرقام حسب السعر

**الفلاتر المتاحة:**
- الكل / يمن موبايل / سبأفون
- البحث النصي
- الترتيب: الأحدث / السعر من الأقل للأعلى / السعر من الأعلى للأقل

---

## 🔧 التعديلات الشائعة

### تغيير اللون الأساسي
**الملف:** `client/src/index.css`
```css
:root {
  --primary: #0EA5E9;  /* غيّر هنا */
}
```

### تغيير كلمة المرور
**الملف:** `client/src/hooks/useNumbersStorage.ts`
```typescript
const ADMIN_PASSWORD = "admin123";  // غيّر هنا
```

### تغيير رقم الواتساب
**الملف:** `client/src/components/SellNumberForm.tsx`
```typescript
const WHATSAPP_NUMBER = "+967711411141";  // غيّر هنا
```

### تغيير معلومات التواصل
**الملف:** `client/src/components/ContactSection.tsx`
```typescript
// غيّر البريد والهاتف والموقع هنا
```

---

## 💾 نظام التخزين

### كيف يعمل localStorage؟
- البيانات تُحفظ في متصفح المستخدم
- لا تحتاج إلى خادم أو قاعدة بيانات
- البيانات تبقى حتى لو أغلق المتصفح

### مفتاح التخزين:
```typescript
const STORAGE_KEY = "yemen_numbers_market_data";
```

### البيانات الافتراضية:
عند فتح الموقع لأول مرة، يتم تحميل 6 أرقام افتراضية من `DEFAULT_NUMBERS` في `useNumbersStorage.ts`

---

## 🎯 تدفق البيانات

```
Home.tsx
  ↓
useNumbersStorage.ts (تحميل البيانات)
  ↓
FilterBar.tsx (تصفية وترتيب)
  ↓
NumberCard.tsx (عرض الأرقام)

AdminDashboard.tsx
  ↓
useNumbersStorage.ts (إضافة/حذف/تعديل)
  ↓
localStorage (حفظ البيانات)
```

---

## 🚀 خطوات الإطلاق

1. **اختبر الموقع محلياً:**
   ```bash
   pnpm dev
   ```

2. **تحقق من عدم وجود أخطاء:**
   ```bash
   pnpm check
   ```

3. **أضف أرقامك الخاصة:**
   - ادخل إلى لوحة التحكم
   - أضف الأرقام الخاصة بك

4. **نشر الموقع:**
   - اضغط على زر "Publish" في لوحة التحكم

---

## 📞 الملفات المهمة للتعديل

| الملف | الغرض | الصعوبة |
|------|-------|--------|
| `Home.tsx` | تغيير النصوص والعناوين | سهل |
| `index.css` | تغيير الألوان والأنماط | متوسط |
| `useNumbersStorage.ts` | تغيير كلمة المرور | سهل |
| `SellNumberForm.tsx` | تغيير رقم الواتساب | سهل |
| `AdminDashboard.tsx` | تعديل لوحة التحكم | متقدم |

---

**آخر تحديث:** فبراير 2026
